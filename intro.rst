Introduction
============

Diesel is a domain specific and expression-oriented language used to generate messages from data that has been sent to Mercurion. These messages are then sent to individuals (e.g. e-mail or SMS messages) or to other systems (e.g. HTTP request messages to a URL).